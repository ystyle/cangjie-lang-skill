/**
 * 示例名称：变量声明与使用
 * 语法模块：第2.1节 - 变量声明
 * 难度：beginner
 * ⚠️ 易错点：无
 *
 * 本示例演示：
 * - let 不可变变量的声明和使用
 * - var 可变变量的声明和修改
 * - 变量类型标注
 * - 字符串插值（使用${}）
 */
main() {
    let a: Int64 = 20      // 不可变变量
    var b: Int64 = 12      // 可变变量
    b = 23                // 可以修改var变量

    println("${a}${b}")    // 输出: 2023
}
